<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "../../dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xcurenet.sqlmap.mappers.mysql.regexPattern">

	<!--	정규식 패턴 등록-->
	<insert id="insertRegexPattern" parameterType="com.xcurenet.regexPattern.service.RegexPatternVO">
		INSERT INTO UI_REGEX_PATTERN (REGEX_NAME, REGEX_PATTERN,REGEX_USER,REGEX_DT)
		VALUES(#{regexPatternName}, #{regexPattern},#{regexUser} ,NOW())
	</insert>

	<!--	정규식 패턴 조회-->
	<select id = "getRegexPatternList" resultType="com.xcurenet.regexPattern.service.RegexPatternVO" parameterType="hashMap">
		SELECT REGEX_NAME AS regexPatternName, REGEX_PATTERN AS regexPattern,REGEX_USER AS regexUser, REGEX_DT AS regexDt, REGEX_SEQ AS regexSeq
		FROM UI_REGEX_PATTERN
		<if test='searchStr!=null and searchStr!=""'>
			WHERE REGEX_NAME  LIKE CONCAT('%', #{searchStr}, '%')
		</if>
		ORDER BY REGEX_SEQ
		<if test='offset!=null and limit > 0'>
			LIMIT	${offset}, ${limit}
		</if>
	</select>

	<!--정규식 패턴 삭제-->
	<update id="deleteRegexPattern" parameterType="com.xcurenet.regexPattern.service.RegexPatternVO">
		DELETE FROM UI_REGEX_PATTERN WHERE REGEX_NAME = #{regexPatternName}
	</update>

	<!--정규식 패턴 수정-->
	<update id="updateRegex" parameterType="com.xcurenet.regexPattern.service.RegexPatternVO">
		UPDATE UI_REGEX_PATTERN
		SET REGEX_NAME = #{regexPatternName},
		REGEX_PATTERN = #{regexPattern},
		REGEX_DT = NOW()
		WHERE REGEX_SEQ = #{regexSeq}
	</update>

	<!--정규식 패턴 이름 중복 확인-->
	<select id="isRegexPatternName" resultType="int" parameterType="com.xcurenet.regexPattern.service.RegexPatternVO">
		SELECT  COUNT(*) AS CNT
		FROM	UI_REGEX_PATTERN
		WHERE	REGEX_PATTERN = #{regexPatternName}
	</select>

</mapper>
