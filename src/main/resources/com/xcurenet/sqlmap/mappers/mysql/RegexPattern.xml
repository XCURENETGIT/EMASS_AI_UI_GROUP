<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "../../dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xcurenet.sqlmap.mappers.mysql.regexPattern">
	<select id = "getPatternList" resultType="patternVO" parameterType="hashMap">
		SELECT CODE AS code, REGEX AS regex, NAME AS name, ENABLE AS enable, CODE_TYPE AS type
		FROM UI_REGEXP
		<if test='searchStr!=null and searchStr!=""'>
			WHERE NAME  LIKE CONCAT('%', #{searchStr}, '%')
		</if>
		ORDER BY (CASE WHEN REGEX IS NULL THEN 0 ELSE 1 END), CODE
		<if test='offset!=null and limit > 0'>
			LIMIT	${offset}, ${limit}
		</if>
	</select>

	<insert id="insertPattern" parameterType="patternVO">
		INSERT INTO UI_REGEXP (CODE, NAME,REGEX,ENABLE,CODE_TYPE)
		VALUES (#{code}, #{name}, #{regex}, #{enable}, #{type})
	</insert>

	<select id="isPatternName" resultType="int" parameterType="patternVO">
		SELECT  COUNT(*) AS CNT
		FROM	UI_REGEXP
		WHERE	CODE = #{code}
	</select>

	<update id="deletePattern" parameterType="patternVO">
		DELETE FROM UI_REGEXP WHERE CODE = #{code}
	</update>


	<update id="updatePattern" parameterType="patternVO">
		UPDATE UI_REGEXP
		SET NAME = #{name},
		<if test='regex != null and regex != ""'>
		REGEX = #{regex},
		</if>
		ENABLE = #{enable}
		WHERE CODE = #{code}
	</update>

	<select id ="allPatternCode" resultType="patternVO">
		SELECT
			CODE AS code,
			CODE_TYPE AS type,
			NAME AS name,
		    ENABLE AS enable
		FROM UI_REGEXP
	</select>

</mapper>
